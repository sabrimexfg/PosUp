import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "Sabrimex - Minnesota's Premier Snack Wholesaler",
    template: "%s | Sabrimex",
  },
  description:
    "Sabrimex is a leading wholesale distributor of authentic Mexican snacks, serving Minnesota and Wisconsin. Home of PosUp, the complete point of sale solution.",
  icons: {
    icon: "/images/Sabrimex.png",
    apple: "/images/Sabrimex.png",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
